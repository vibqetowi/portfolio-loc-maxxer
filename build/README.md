# Build Directory

This directory contains the compiled WebAssembly binaries.

Run `npm run asbuild` to generate:
- `sim.wasm` - Optimized production binary
- `sim.wat` - Human-readable WebAssembly text format
- `sim.debug.wasm` - Debug version with source maps

